#include <stdio.h>

main(){
 char resp;

  resp=funcao(10,10);
  printf("resultado e: %c\n", resp);

}
int funcao(int x,int y){
    if (x=y){
        return 73;
    }
    else {
        return 0;
    }
}
